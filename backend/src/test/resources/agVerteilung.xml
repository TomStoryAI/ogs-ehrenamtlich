<solver>
    <scanAnnotatedClasses/>
    <scoreDirectorFactory>
        <easyScoreCalculatorClass>de.children.agplanner.optaplanner.AGVerteilungEasyScore</easyScoreCalculatorClass>
    </scoreDirectorFactory>

    <termination>
        <secondsSpentLimit>1</secondsSpentLimit>
    </termination>
</solver>