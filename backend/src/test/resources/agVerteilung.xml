<solver>
    <scanAnnotatedClasses/>
    <scoreDirectorFactory>
        <easyScoreCalculatorClass>de.children.agplanner.optaplanner.AGPlannerConstraintProvider</easyScoreCalculatorClass>
    </scoreDirectorFactory>

    <termination>
        <secondsSpentLimit>1</secondsSpentLimit>
    </termination>
</solver>